# Fixtures

This directory is for redacted, replayable evidence fixtures.

Default validation must not require live Google Flights. Live refreshes belong
in task-scoped artifact directories and become fixtures only after redaction and
review.
